import { createTheme } from '@mui/material/styles';

const themeColor = createTheme({
    palette: {
      primary: {
        main: "#064e58",
      }, 
      secondary: {
        main: "#fff ",
      }
    },
  });
  
  export default themeColor